name: static analysis

on:
  pull_request:
    branches:
      - develop
      - main
      - release/**
      - feature/**
      - hotfix/**
  push:
    branches:
      - develop
      - main
      - release/**
      - feature/**
      - hotfix/**

jobs:
  pylint:
    name: pylint
    strategy:
      fail-fast: false
      matrix:
        python_ver: [3.9]
    runs-on: windows-latest
    env:
      PYTHONUTF8: 1
      CONDA_LOCK_ENV_FILE: conda-py-${{ matrix.python_ver }}-win-64-dev.lock.yml
    steps:
      - uses: actions/checkout@v2
      - name: Cache conda
        uses: actions/cache@v2
        env:
          # Increase this value to reset cache if environment.yml has not changed
          CACHE_NUMBER: 1
        with:
          path: ~/conda_pkgs_dir
          key:
            ${{ runner.os }}-conda-${{ env.CACHE_NUMBER }}-py-${{ matrix.python_ver }}-${{ hashFiles(env.CONDA_LOCK_ENV_FILE) }}
      - name: Setup conda env
        uses: conda-incubator/setup-miniconda@v2
        with:
          activate-environment: geoapps
          python-version: ${{ matrix.python_ver }}
          channel-priority: flexible
          environment-file: ${{ env.CONDA_LOCK_ENV_FILE }}
          use-only-tar-bz2: true # IMPORTANT: This needs to be set for caching to work properly!
      - name: Run pylint on modified files
        if: github.event_name == 'pull_request'
        shell: bash -l {0}
        run: >-
          conda activate geoapps

          pylint $(
          git diff --diff-filter=AM --name-only refs/remotes/origin/${{github.base_ref}}... | grep -E '^(geoapps|tests)/.*\.py$'
          || echo 'geoapps/__init__.py'
          )
      - name: Run pylint on all files
        if: github.event_name == 'push'
        shell: bash -l {0}
        run: |
          conda activate geoapps
          pylint geoapps tests
